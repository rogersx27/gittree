# Los paquetes del workspace se enlazan entre si en vez de bajarse del registry
link-workspace-packages=true

# Falla si el lockfile no coincide con los package.json (util en CI)
strict-peer-dependencies=false

# Mantiene node_modules aislado: nada de dependencias fantasma
shamefully-hoist=false
